spring:
  application:
    name: vaarahidesigns
  datasource:
    url: jdbc:postgresql://localhost:5432/vaarahidesigns
    username: sreekarchowdhary
    password: ""
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
